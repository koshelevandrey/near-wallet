export interface IconsMapper extends Record<string, unknown> {
  arrowGroup: string;
  arrowGrey: string;
  nearMenu: string;
  omniLogo: string;
  success: string;
  tokenA: string;
  arrow: string;
  }